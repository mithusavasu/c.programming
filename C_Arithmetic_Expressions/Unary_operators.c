/*Unary Operators - one operand
     Unary Plus (+)   +2 = 2
     Unary Minus (-)  -(-2) = 2
*/

#include <stdio.h>
int main(void) 
{
    int x = 3;
    printf("x = %d\n", x); //output x = -3
    printf("-x = %d\n", -x); //output -x = 3
    printf ("+x = %d\n", +x); //output +x = -3
    
}